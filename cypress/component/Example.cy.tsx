// Since there is unit test for components using Jest in this app, Cypress component tests seems redundant. Leaving this file in repo as an example.

describe("Example.cy.tsx", () => {
  it("playground", () => {
    // cy.mount()
  });
});
